<%= form_for([@article, @article.comments.new]) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :reply %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Post a reply!' %>
  </div>
<% end %>
